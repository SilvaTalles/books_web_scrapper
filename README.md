# books_web_scrapper
A Python web scraper built for portfolio purposes that extracts book data from Books to Scrape. It uses httpx for requests, selectolax for fast HTML parsing, and dataclasses for structured storage. The scraper collects titles, categories, prices, taxes, UPCs, and ratings, exporting everything into a clean CSV file.
